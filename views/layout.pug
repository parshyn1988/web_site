doctype html
html(lang='ru')
	head
		meta(charset='UTF-8')
		meta(name='viewport' content='width=device-width, initial-scale=1')
		title= title
		link(rel='stylesheet' href='/css/style.css')
		link(rel='stylesheet' href='https://fonts.googleapis.com/css?family=Inconsolata')
		style.
			body, html {
				height: 100%;
				font-family: "Inconsolata", sans-serif;
			}
			.bgimg {
				background-position: center;
				background-size: cover;
				background-image: url("/i/coffeehouse.jpg");
				min-height: 75%;
			}
			.menu {
				display: none;
			}
	body
		// Links (sit on top)
		div.w3-top
			div.w3-row.w3-padding.w3-black
				div.w3-col.s3
					a.w3-button.w3-block.w3-black(href='/') HOME
				div.w3-col.s3
					a.w3-button.w3-block.w3-black(href='/about') ABOUT
				div.w3-col.s3
					a.w3-button.w3-block.w3-black(href='/menu') MENU
				div.w3-col.s3
					a.w3-button.w3-block.w3-black(href='/contact') CONTACT
		// Main content
		block content
		// Footer
		footer.w3-center.w3-light-grey.w3-padding-64.w3-large
			p Powered by
				|
				|
				a.w3-hover-text-green(href='https://www.w3schools.com/' title='W3.CSS') w3.css
		script.
			// Tabbed Menu
			function openMenu(evt, menuName) {
				var i, x, tablinks;
				x = document.getElementsByClassName("menu");
				for (i = 0; i < x.length; i++) {
					x[i].style.display = "none";
				}
				tablinks = document.getElementsByClassName("tablink");
				for (i = 0; i < x.length; i++) {
					tablinks[i].className = tablinks[i].className.replace(" w3-dark-grey", "");
				}
				document.getElementById(menuName).style.display = "block";
				evt.currentTarget.firstElementChild.className += " w3-dark-grey";
			}